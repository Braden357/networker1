/** Max candidates materialized per campaign run (spec §9, §15). */
export const MAX_CANDIDATES_PER_RUN = 50;

/** MVP: max completed runs per user per rolling 7 days (spec §15). */
export const MAX_COMPLETED_RUNS_PER_WEEK = 2;
